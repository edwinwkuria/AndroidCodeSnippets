1. Go to the manifest.Xml file

Add an intent filter:
            <intent-filter>
                <action android:name="android.intent.action.{{Name of action}}" />

                <category android:name="android.intent.category.{{ Default}}" />
                check for other requirements such as data
            </intent-filter>
